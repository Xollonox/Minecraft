# CLAUDE — Project Notes

## User Commands
- When user says **"see ss"** → automatically read the latest image from `/storage/emulated/0/DCIM/Screenshots/` (sort by mtime, pick newest `.jpg`/`.png`). Use Read tool to view it — do not ask user to upload again.
